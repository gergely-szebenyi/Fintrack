package com.prekogdevs.fintrack.core.theme

import androidx.compose.ui.graphics.Color
import com.prekogdevs.fintrack.core.design.ColorTokens

object AppColors {

    // Brand
    val BluePrimary = Color(ColorTokens.BLUE_PRIMARY)
    val BlueLight = Color(ColorTokens.BLUE_LIGHT)
    val BlueDark = Color(ColorTokens.BLUE_DARK)

    // Semantic
    val GreenIncome = Color(ColorTokens.GREEN_INCOME)
    val RedExpense = Color(ColorTokens.RED_EXPENSE)

    // Surfaces
    val BgGray = Color(ColorTokens.BG_GRAY)
    val CardWhite = Color(ColorTokens.CARD_WHITE)

    // Text
    val TextPrimary = Color(ColorTokens.TEXT_PRIMARY)
    val TextSecondary = Color(ColorTokens.TEXT_SECONDARY)

    // Charts
    val Food = Color(ColorTokens.FOOD)
    val Transport = Color(ColorTokens.TRANSPORT)
    val Shopping = Color(ColorTokens.SHOPPING)
    val Coffee = Color(ColorTokens.COFFEE)
    val Health = Color(ColorTokens.HEALTH)
}
